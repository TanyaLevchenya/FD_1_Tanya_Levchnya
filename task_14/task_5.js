// Создайте программу, которая будет генерировать массив с числами от 1 до вводимого числа

let a = prompt();
let c = prompt();

let arr = [];

    for (let i = a; i <= c; i++){
      arr.push(i);
    }
console.log(arr);